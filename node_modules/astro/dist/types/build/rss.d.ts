import type { CollectionRSS } from '../@types/astro';
/** Validates createCollection.rss */
export declare function validateRSS(rss: CollectionRSS, srcFile: string): void;
declare type RSSInput<T> = {
    data: T[];
    site: string;
} & CollectionRSS<T>;
interface RSSOptions {
    srcFile: string;
    feedURL: string;
}
/** Generate RSS 2.0 feed */
export declare function generateRSS<T>(input: RSSInput<T>, options: RSSOptions): string;
export {};
